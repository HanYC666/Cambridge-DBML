{
  "name": "cambridge-dbml-static",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --exclude=e2e/**",
    "test:browser": "playwright test",
    "build": "vite build",
    "dev": "vite --host 127.0.0.1",
    "preview": "vite preview --host 127.0.0.1"
  },
  "devDependencies": {
    "playwright": "^1.55.0",
    "vite": "^7.1.0",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "sql.js": "^1.14.2"
  }
}
