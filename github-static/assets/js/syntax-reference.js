export const SYNTAX_REFERENCE = [
    { category: "DDL", name: "CREATE DATABASE", summary: "Creates a new, empty database.", syntax: "CREATE DATABASE database_name;", example: "CREATE DATABASE Rentals;", notes: "Rarely asked for marks on its own — usually appears as one line in a larger DDL script." },
    { category: "DDL", name: "CREATE TABLE", summary: "Defines a new table: its field names, datatypes, and constraints (primary key, foreign key, NOT NULL).", syntax: "CREATE TABLE TableName\n(\n    Field1 DATATYPE,\n    Field2 DATATYPE NOT NULL,\n    ...\n    PRIMARY KEY (Field1),\n    FOREIGN KEY (Field2) REFERENCES OtherTable(Field)\n);", example: "CREATE TABLE Customer\n(\n    CustomerID INTEGER,\n    Name VARCHAR(30) NOT NULL,\n    JoinDate DATE,\n    PRIMARY KEY (CustomerID)\n);", notes: "Field list must be inside matching brackets, comma-separated. Keys are defined as their own lines at the end." },
    { category: "DDL", name: "ALTER TABLE ... ADD", summary: "Adds a new field to an existing table.", syntax: "ALTER TABLE TableName ADD FieldName DATATYPE;", example: "ALTER TABLE Customer ADD PhoneNumber VARCHAR(15);", notes: "Only ADD is in the 9618 subset — DROP COLUMN / MODIFY are not expected." },
    { category: "DDL", name: "PRIMARY KEY", summary: "Declares the field (or fields) that uniquely identify each row in the table.", syntax: "PRIMARY KEY (FieldName)", example: "PRIMARY KEY (CustomerID)", notes: "Must use exactly this form inside the CREATE TABLE brackets — not a separate statement." },
    { category: "DDL", name: "FOREIGN KEY", summary: "Declares a field that links to the primary key of another table, enforcing referential integrity.", syntax: "FOREIGN KEY (FieldName) REFERENCES OtherTable(OtherField)", example: "FOREIGN KEY (CustomerID) REFERENCES Customer(CustomerID)", notes: "The REFERENCES clause is required — 'FOREIGN KEY (field)' on its own is incomplete and will be marked wrong." },
    { category: "DDL", name: "NOT NULL", summary: "Forces a field to always have a value — it cannot be left empty.", syntax: "FieldName DATATYPE NOT NULL", example: "Name VARCHAR(30) NOT NULL", notes: "Written directly after the datatype, inside the CREATE TABLE field list." },
    { category: "DDL", name: "Data types", summary: "The only datatype names CIE 9618 recognises for CREATE TABLE fields.", syntax: "INTEGER | REAL | CHARACTER(n) | VARCHAR(n) | BOOLEAN | DATE | TIME", example: "Price REAL, Code CHARACTER(5), Description VARCHAR(50)", notes: "Do NOT use INT, FLOAT, TEXT, STRING, BOOL, DATETIME, NUMBER — these are common non-Cambridge names the linter will flag." },
    { category: "DML", name: "SELECT ... FROM", summary: "Retrieves fields from a table. Use * to retrieve all fields.", syntax: "SELECT Field1, Field2 FROM TableName;", example: "SELECT Name, JoinDate FROM Customer;", notes: "Every query starts with SELECT and FROM — they're the only compulsory clauses." },
    { category: "DML", name: "WHERE", summary: "Filters rows so only those matching a condition are returned.", syntax: "SELECT ... FROM TableName WHERE condition;", example: "SELECT * FROM Customer WHERE JoinDate > '2024-01-01';", notes: "Text and date literals MUST be in single quotes, e.g. WHERE Name = 'Smith'. Numbers are not quoted." },
    { category: "DML", name: "ORDER BY", summary: "Sorts the result set by one or more fields, ascending by default.", syntax: "SELECT ... FROM TableName ORDER BY Field [ASC|DESC];", example: "SELECT * FROM Customer ORDER BY Name ASC;", notes: "Goes after WHERE (if present)." },
    { category: "DML", name: "GROUP BY", summary: "Groups rows that share the same value in a field, typically for use with SUM/COUNT/AVG.", syntax: "SELECT Field, AggregateFunction(...) FROM TableName GROUP BY Field;", example: "SELECT CustomerID, COUNT(*) FROM Rental GROUP BY CustomerID;", notes: "Every non-aggregated field in SELECT should appear in GROUP BY." },
    { category: "DML", name: "INNER JOIN ... ON", summary: "Combines rows from two tables where a related field matches in both.", syntax: "SELECT ... FROM TableA INNER JOIN TableB ON TableA.Field = TableB.Field;", example: "SELECT Customer.Name, Rental.MonthlyCost\nFROM Customer INNER JOIN Rental\nON Customer.CustomerID = Rental.CustomerID;", notes: "9618 scripts are limited to at most two tables. LEFT/RIGHT/FULL/OUTER/CROSS JOIN are outside the syllabus subset." },
    { category: "DML", name: "SUM / COUNT / AVG", summary: "Aggregate functions: total a numeric field, count rows, or average a numeric field.", syntax: "SELECT SUM(Field) | COUNT(Field) | AVG(Field) FROM TableName;", example: "SELECT AVG(MonthlyCost) FROM Rental;", notes: "COUNT(*) counts rows regardless of NULLs; COUNT(Field) ignores NULLs in that field." },
    { category: "DML", name: "INSERT INTO ... VALUES", summary: "Adds a new row to a table.", syntax: "INSERT INTO TableName (Field1, Field2) VALUES (Value1, Value2);", example: "INSERT INTO Customer (CustomerID, Name, JoinDate) VALUES (1, 'Smith', '2024-01-01');", notes: "Text/date values in quotes; number of values must match the field list, in the same order." },
    { category: "DML", name: "DELETE FROM", summary: "Removes rows from a table that match a condition.", syntax: "DELETE FROM TableName WHERE condition;", example: "DELETE FROM Customer WHERE CustomerID = 1;", notes: "Omitting WHERE deletes every row in the table — always double-check the condition." },
    { category: "DML", name: "UPDATE ... SET", summary: "Changes existing values in one or more fields for rows that match a condition.", syntax: "UPDATE TableName SET Field1 = Value1 WHERE condition;", example: "UPDATE Customer SET Name = 'Smyth' WHERE CustomerID = 1;", notes: "Like DELETE, a missing WHERE clause updates every row in the table." }
];
